type objtype = {name:string, age:number, email:string}

const person:objtype={
    name: 'Shashank',
    age: 23,
    email: 'sda@sfdf.com'
};
console.log(person.name)

// if we want multiple object with same fields as name age and email we can define it at the top and use as data type with our object variables

const user:objtype ={
    name: 'Shashank',
    age: 23,
    email: 'sda@sfdf.com'
};
console.log(user.name)








//interview quesion:

// what is object type and how can you make it?
// showed above